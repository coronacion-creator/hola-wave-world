# ==================================
# Variables de Entorno - Ejemplo
# ==================================
# Este archivo muestra las variables necesarias para el proyecto.
# NO incluyas valores reales aquí. Copia este archivo como .env
# y completa con tus credenciales reales.

# ----------------------------------
# PostgreSQL (Supabase / Lovable Cloud)
# ----------------------------------
# URL base del proyecto Supabase
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave anónima (pública) - Safe para frontend
VITE_SUPABASE_PUBLISHABLE_KEY=tu-clave-publica-aqui

# ID del proyecto Supabase
VITE_SUPABASE_PROJECT_ID=tu-project-id

# ----------------------------------
# MongoDB Atlas (NoSQL)
# ----------------------------------
# URI de conexión a MongoDB Atlas
# Formato: mongodb+srv://<usuario>:<password>@cluster.xxx.mongodb.net/<dbname>?retryWrites=true&w=majority
VITE_MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/school_management?retryWrites=true&w=majority

# ----------------------------------
# Secretos para Edge Functions (Backend)
# ----------------------------------
# IMPORTANTE: Estas variables solo deben usarse en Edge Functions
# NO las expongas en el frontend

# Service Role Key de Supabase (NUNCA en frontend)
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key-aqui

# URL de Supabase (para edge functions)
SUPABASE_URL=https://tu-proyecto.supabase.co

# MongoDB URI (para edge functions)
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/school_management

# ----------------------------------
# Opcional: Servicios Externos
# ----------------------------------
# Resend (envío de emails)
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxx

# ----------------------------------
# Notas de Seguridad
# ----------------------------------
# 1. NUNCA commits valores reales en Git
# 2. SUPABASE_SERVICE_ROLE_KEY solo en servidor/edge functions
# 3. En producción, usa variables de entorno seguras
# 4. Rota credenciales periódicamente
# 5. Usa MongoDB IP Whitelist en producción
